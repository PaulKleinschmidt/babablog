<span>+{{likes.length}}</span>




{{#if auth.isAuthenticated}}


{{!-- Uncomment to Display users who liked post --}}


{{!-- Liked by:
{{#each likes as |like|}}
{{like.user.email}}
{{/each}} --}}

{{!-- Check if a like has an editable property. If it does, display deleteLike button --}}

{{#each likes as |like|}}
  {{#if like.editable}}
    <button class="delete-like-button" {{action 'deleteLike' like}}>👎</button>

    {{else}}
    {{/if}}
{{/each}}




{{#unless likedByUser}}
  <button class="like-button" {{action 'addLike' post}}>👍</button>
{{/unless}}
{{/if}}
